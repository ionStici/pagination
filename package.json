{
  "scripts": {
    "live": "live-server",
    "sass": "sass src/sass/main.scss src/css/style.css -w",
    "start": "npm-run-all --parallel live sass"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.56.1"
  }
}
